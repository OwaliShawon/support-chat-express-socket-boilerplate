const authService = {
    register: async (req, res, next) => {
        try {
            return;
        } catch (error) {
            throw error;
        }
    },
    login: async (req, res, next) => {
        try {
            return;
        } catch (error) {
            throw error;
        }
    },
};

module.exports = authService;
